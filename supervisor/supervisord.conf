[supervisord]
nodaemon=true
logfile=/home/ubuntu/.supervisor/supervisord.log
pidfile=/home/ubuntu/.supervisor/supervisord.pid
# user=root  <--- Make sure this line is removed or commented out

[include]
files = /opt/supervisor/conf.d/*.conf