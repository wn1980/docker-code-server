# Run this command in your terminal in the project directory:
# echo "HOST_DOCKER_GID=$(getent group docker | cut -d: -f3 || echo 999)" > .env
# Replace 999 with a sensible default if the command fails or docker group doesn't exist
HOST_DOCKER_GID=YOUR_ACTUAL_DOCKER_GID
