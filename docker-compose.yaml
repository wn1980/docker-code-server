services:
  code-server:
    build: .
    image: wn1980/code-server-ai
    container_name: code_server_ai
    environment:
      - TZ=Asia/Bangkok # Timezone set previously
    volumes:
      - config:/home/ubuntu/.config
      - projects:/home/ubuntu/project
    ports:
      - "8443:8443"
    restart: unless-stopped
    # user: "1000:1000" # Removed this line

volumes:
  config:
  projects: